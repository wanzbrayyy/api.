<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body>
    <%- include('partials/navbar', { user: user }) %>

    <div class="main-wrapper container animate__animated animate__fadeInUp">
        
        <div class="dashboard-grid">
            <div class="glass-panel stat-card">
                <div class="stat-icon" style="background: rgba(79, 70, 229, 0.2); color: #818cf8;">
                    <i class="fa-solid fa-user-astronaut"></i>
                </div>
                <div>
                    <h3 style="margin: 0; font-size: 1.1rem;">Hello, <%= user.name %></h3>
                    <p style="margin: 0; color: #94a3b8; font-size: 0.85rem;">Welcome back!</p>
                </div>
            </div>

            <div class="glass-panel stat-card">
                <div class="stat-icon" style="background: rgba(236, 72, 153, 0.2); color: #f472b6;">
                    <i class="fa-solid fa-signal"></i>
                </div>
                <div>
                    <h3 style="margin: 0; font-size: 1.1rem;">System Status</h3>
                    <p style="margin: 0; color: #4ade80; font-size: 0.85rem;">● Online (API Only)</p>
                </div>
            </div>
        </div>

        <div class="glass-panel interactive-zone">
            <div class="zone-header">
                <h3 style="margin: 0; font-size: 1rem;">Interactive Zone</h3>
                <p style="color: #94a3b8; font-size: 0.8rem; margin: 0;">Visualize space (Client-Side)</p>
            </div>
            
            <div style="display: flex; justify-content: center; align-items: center; flex: 1; opacity: 0.1; padding: 40px;">
                <i class="fa-solid fa-globe fa-spin" style="font-size: clamp(8rem, 20vw, 15rem); animation-duration: 20s;"></i>
            </div>
        </div>
    </div>
</body>
</html>