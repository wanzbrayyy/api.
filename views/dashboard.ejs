<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body>
    <%- include('partials/navbar', { user: user }) %>

    <div class="main-wrapper container animate__animated animate__fadeInUp">
        
        <div class="dashboard-grid">
            <div class="glass-panel stat-card">
                <div class="stat-icon" style="background: rgba(79, 70, 229, 0.2); color: #818cf8;">
                    <i class="fa-solid fa-user-astronaut"></i>
                </div>
                <div>
                    <h3 style="margin: 0; font-size: 1.1rem;"><%= t.welcome %>, <%= user.name %></h3>
                    <p style="margin: 0; color: #94a3b8; font-size: 0.85rem;"><%= t.welcome_sub %></p>
                </div>
            </div>

            <div class="glass-panel stat-card">
                <div class="stat-icon" style="background: rgba(16, 185, 129, 0.2); color: #34d399;">
                    <i class="fa-solid fa-server"></i>
                </div>
                <div>
                    <h3 style="margin: 0; font-size: 1.1rem;"><%= t.system %></h3>
                    <p style="margin: 0; color: #94a3b8; font-size: 0.85rem;">Status: <span style="color: #34d399;"><%= t.online %></span></p>
                </div>
            </div>
        </div>

        <div class="glass-panel" style="flex: 1; padding: 30px; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; min-height: 400px;">
            <div style="background: rgba(255,255,255,0.05); padding: 20px; border-radius: 50%; margin-bottom: 20px;">
                <i class="fa-solid fa-globe fa-spin" style="font-size: 5rem; color: rgba(255,255,255,0.2); animation-duration: 10s;"></i>
            </div>
            <h3><%= t.content_title %></h3>
            <p style="color: #94a3b8; max-width: 400px;">
                <%= t.content_desc %>
            </p>
        </div>
    </div>
</body>
</html>