<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>
<body>
    <%- include('partials/navbar', { user: user }) %>

    <div class="main-wrapper auth-container">
        <div class="glass-panel auth-box animate__animated animate__zoomIn">
            <div class="form-header">
                <h2 class="gradient-text">Join Squad</h2>
                <p style="color: #94a3b8;">Create your account to get started.</p>
            </div>

            <form action="/api/auth/register" method="POST">
                <div class="input-group">
                    <i class="fa-solid fa-user input-icon"></i>
                    <input type="text" name="name" placeholder="Full Name" required class="input-modern">
                </div>

                <div class="input-group">
                    <i class="fa-solid fa-envelope input-icon"></i>
                    <input type="email" name="email" placeholder="Email Address" required class="input-modern">
                </div>

                <div class="input-group">
                    <i class="fa-solid fa-lock input-icon"></i>
                    <input type="password" name="password" placeholder="Password" required class="input-modern">
                </div>

                <button type="submit" class="btn-modern" style="width: 100%; padding: 14px;">
                    Create Account <i class="fa-solid fa-rocket"></i>
                </button>
            </form>

            <div style="text-align: center; margin-top: 20px; color: #94a3b8;">
                Already have an account? <a href="/login" style="color: var(--secondary);">Login here</a>
            </div>
        </div>
    </div>

    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
    <% if (error) { %>
    <script>
        Toastify({
            text: "<%= error %>",
            duration: 3000,
            gravity: "top",
            position: "right",
            style: { background: "linear-gradient(to right, #ff5f6d, #ffc371)" }
        }).showToast();
    </script>
    <% } %>
</body>
</html>